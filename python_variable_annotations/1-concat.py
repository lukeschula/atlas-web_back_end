#!/usr/bin/env python3
""" concat that takes a string str1 and a string str2 as arguments"""


def concat(str1: str, str2: str) -> str:
    """take two strings and returns a string"""
    return str1 + str2